@model IEnumerable<FelveteliFeladat.Models.Team>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FoundationYear)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumberOfChampionshipWins)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsPayedTheEntryFee)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FoundationYear)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumberOfChampionshipWins)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsPayedTheEntryFee)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
